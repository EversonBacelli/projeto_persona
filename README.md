Comece abrindo o terminal e colocando "npm start" para automaticamente baixar o live server,Prettier e abrir o Live Server.

Dessa forma você não precisa instalar nada manualmente.

O live server abre uma janela no navegador com o projeto rodando localmente.

e sempre que sair do projeto use "npm format" para organizar TODOS os códigos.
