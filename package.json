{
  "name": "persona",
  "version": "1.0.0",
  "description": "Projeto Persona",
  "main": "index.js",
  "scripts": {
    "start": "node setup.js && npx live-server",
    "format": "npx prettier --write ."
  },
  "devDependencies": {
    "prettier": "^3.6.2"
  }
}
